# Enforce LF line endings for all text files in this repo.
# This prevents CRLF bleed-in on Windows/WSL checkouts.
* text=auto eol=lf

# Explicitly binary – never touch line endings
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.pdf binary
